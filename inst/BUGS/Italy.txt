model italy;
    const
        N=16;
    var
        y[N],alpha,beta,lambda[N];
    data y in "italy.dat";
    inits in "italy.in";
{
    for (i in 1:N) {
        lambda[i] ~ dgamma(alpha,beta);
        y[i] ~ dpois(lambda[i]);
    }
    alpha ~ dgamma(1,1);
    beta ~ dgamma(1,1);
}
